<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- New Menu Item for MQTT Host IP -->
    <item
        android:id="@+id/action_set_host"
        android:title="Set MQTT Host"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <!-- New Menu Item for MQTT Topic -->
    <item
        android:id="@+id/action_set_topic"
        android:title="Set MQTT Topic"
        android:orderInCategory="101"
        app:showAsAction="never" />

    <!-- New Menu Item for Publishing a Message -->
    <item
        android:id="@+id/action_publish_message"
        android:title="Publish Message"
        android:orderInCategory="102"
        android:icon="@android:drawable/ic_menu_send"
        app:showAsAction="ifRoom" />

</menu>


